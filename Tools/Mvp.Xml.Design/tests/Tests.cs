using System;
using System.Xml;

namespace Mvp.Xml.Design.Tests
{
	class Tests
	{
		public void Run()
		{
			//CustomerSchemaReader reader = new CustomerSchemaReader();
			//reader.CustomerSchemaDeserialized += delegate { Console.WriteLine("CUstomerSchemaDeserialized"); };

			//CustomerSchemaSerializer serializer = new CustomerSchemaSerializer(reader);
			//CustomerSchema sch = (CustomerSchema)serializer.Deserialize((XmlReader)null);
			//sch.SayHi();
		}
	}
}
